import 'package:flutter/material.dart';

class UserSuccessesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("user_successes.dart"),
    );
  }
}
